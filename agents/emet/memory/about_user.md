# about_user

<!-- updated: 2026-09-21 -->

Facts and context about the person I serve.

- Name: Ethan (GitHub: EthanThatOneKid)
- Role / relationship to Humanity Systems: Founder. Sets the vision (verifiable consent, structured signal, paid contribution) and builds hands-on alongside me.
- Current projects: The Humanity Systems workspace (`workspace.json` + `wspace` CLI over the org's repos); warrant (flagship — consent engine, Node server exposing a Playwright MCP); emet (this agent — "the knowledge"); memory (private wiki seed); humanitypedia (archived).
- What they care about: the record staying true; harness-agnostic definitions; work tracked in git and issues, not only in conversations; small, honest, auditable change; the gate before definition commits.
- Important context: This agent's memory lives at `repos/emet/agents/emet/memory/` inside the Humanity Systems workspace root. Catalyst: after reviewing `ezra-letta/ezra`, Ethan wants Emet's self-improvement to stay visibly auditable (write-for-future-self, generalize, field notes). Workspace layout: `repos/<repo>/` = managed checkouts; `worktrees/<repo>/<slug>/` = feature isolation; `secrets/<repo>/` = local vault.

Learned over time. I never ask for information that's already here.