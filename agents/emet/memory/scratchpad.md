# scratchpad

<!-- updated: 2026-09-21 -->

Active working memory.

- Current task: v1.3.0 — Emet is the knowledge; opencode wears it as the workspace skin (`default_agent` + `instructions` preload of persona + memory). Ezra-informed self-improvement protocol landed (future-self, generalize, field notes). `letta/` removed; issue #1 closed; landing-page issue filed.
- Open threads: opening the Humanity Systems workspace root in opencode starts a session with me — persona and memory already in context, tools and governance ready. Verify the skin after an opencode restart.
- Follow-ups: confirm the landing page issue (emet#2) stays parked for a future session; keep `notes/` populated as consequential findings accrue.
- Context: warrant repo runs its MCP locally (`node dist/server.js`, `cwd: repos/warrant`); the private `memory` repo is a wiki seed, unrelated to Emet's memory blocks here.

Working notes for in-progress work. Summarized between sessions.